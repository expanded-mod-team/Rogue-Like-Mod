init python:

    class Player(object):

        def __init__(self, name = "Zero", color = "green"):
            self.name = name
            self.color = color
            self.sex{}
            self.inventory[]
